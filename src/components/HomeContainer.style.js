import styled from 'styled-components';

export const HomeContainer = styled.div`
  background-color: hsl(207, 26%, 17%);
  padding: 4.8rem 8.2rem;
  display: grid;
  gap: 4.7rem;
  // It makes the homecontainer take over all the available height
  flex-grow: 1;
`;
